# 方案总文档

## P0：头部 PI4 v1.0 自动化

- 状态：已实施，待真机验收（2026-07-28）。
- 分支：`feature/head`，基于 `main`；现有未跟踪上半身文件不纳入本任务。
- 交付：独立 TCP 头部 fixture、25 条可收集参数化用例、`test_data/head.xlsx`、头部专属 Skill 与测试方案。
- 风险：本地 `pytuyarobot 2.0.3` 只支持 2 关节，文档合同要求 4 关节；相关实机场景将明确标为 `xfail`，不归类为设备故障。

## P0：远程 main 分支同步

- 状态：已完成（2026-07-28）
- 目标：更新本地的 `origin/main` 远程跟踪引用，且不影响当前 `feature/chassis` 分支和未跟踪文件。
- 结果：已执行 `git fetch origin main:refs/remotes/origin/main`；`origin/main` 位于 `a2bb92d`（`add upper_body interface`）。

## P1：分支规则与技能配置核对

- 状态：已完成（2026-07-28）
- 结论：`feature/chassis` 并非没有配置：含 `.cursorrules` 与两项 `elephant-*` Skill；但缺少 `AGENTS.md`。`feature/upper_body` 含 `AGENTS.md`、`.cursorrules` 与三项 `tuya-*` Skill，规则针对 TuyaRobot 测试。

## P0：规则与技能治理建议

- 状态：已建议，待确认（2026-07-28）
- 统一基线：将 TuyaRobot 通用安全、目录、fixture、Excel 与测试骨架规则维护在 `main` 的 `AGENTS.md`；将跨模块通用的用例编写技能维护在 `main` 的 `.cursor/skills/tuya-pytest-case-authoring/`。
- 模块扩展：将仅对特定模块生效的流程拆为独立 Skill，例如上半身运动的 `tuya-motion-test-authoring`，后续可增加底盘专用 Skill；由任务需要显式调用，不写入全局强制规则。
- 同步方式：模块分支创建时从 `main` 继承基线；`main` 更新后，模块分支按开发节点定期 rebase 或 merge `main`，并进行冲突审查。不要逐次复制粘贴规则文件。

## P0：Tuya 规则与技能基线整理

- 状态：已完成（2026-07-28）
- 已迁移：`AGENTS.md`、Tuya `.cursorrules`、`tuya-pytest-case-authoring`、`tuya-pytest-registry-session` 与模块专用 `tuya-motion-test-authoring`。
- 已移除：不适用于本仓库的 `elephant-pytest-case-authoring` 与 `elephant-pytest-registry-session`。
- 验证：三项 Skill 均通过 `quick_validate.py`（UTF-8 模式）；`git diff --check` 通过。未运行 pytest，因为本次不修改可执行代码或测试数据。
- 后续：将本次规则与技能变更单独提交至 `main`，模块分支再通过 merge 或 rebase 同步该基线。

## P0：规则与技能基线分支同步

- 状态：已完成（2026-07-28）
- `main`：已包含 Tuya 规则、技能与项目记录提交。
- `feature/upper_body`：已快进同步 `main`。
- `feature/chassis`：已合并 `main`，并保留本分支的规则整理提交。
- 已推送至 `origin`：`main`、`feature/upper_body` 与 `feature/chassis` 均已发布（2026-07-28）。
