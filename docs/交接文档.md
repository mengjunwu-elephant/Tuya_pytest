# 项目交接文档

## 2026-07-28：头部 PI4 v1.0 自动化

- 已在 `feature/head` 实现独立 TCP 头部测试，不再将头部作为 `COM4/115200` 串口设备；默认地址为 `192.168.0.231:6501`。
- 新增 `testcases/head/`、`test_data/head.xlsx`、`tuya-head-test-authoring` 与专项测试方案；原上半身目录的头部主版本测试已迁移。
- 定向验证：`compileall testcases/head` 和 `pytest testcases/head --collect-only -q` 通过，收集 25 条；未启用真机时执行结果为 25 条安全跳过。完整 `compileall testcases` 被既有 `testcases/upper_body/__pycache__` 文件锁阻断。
- 全仓收集新增头部后识别到 848 条可收集用例，但被 9 个用户未跟踪的上半身测试阻断：它们引用的 gripper/安全/异步 sheet 尚未写入 `test_data/upper_body.xlsx`；本任务未修改这些文件。
- 未运行真机。运动/人工/危险接口仍需显式安全门控和现场确认；真实升级、通信中断和 100 次稳定性不在范围内。

## 2026-07-28：同步远程 main

- 当前工作分支：`feature/chassis`。
- 已同步的远程跟踪分支：`origin/main`，提交为 `a2bb92d`（`add upper_body interface`）。
- 未将 `origin/main` 合并或变基到当前分支；这样可避免影响用户现有未跟踪文件。
- 项目内未发现既有规则或技能文件；本次未新增项目规则或技能。

## 2026-07-28：分支规则与技能核对

- `feature/chassis`：`.cursorrules`；`.cursor/skills/elephant-pytest-case-authoring/SKILL.md`；`.cursor/skills/elephant-pytest-registry-session/SKILL.md`。无 `AGENTS.md`。
- `feature/upper_body`：`AGENTS.md`、`.cursorrules`；`tuya-motion-test-authoring`、`tuya-pytest-case-authoring`、`tuya-pytest-registry-session` 三项 Skill。
- 两个分支的规则与 Skill 并非相同；当前分支的 `elephant-*` 配置应视为旧项目上下文，不应当作 TuyaRobot 的完整规则集。

## 2026-07-28：规则与技能治理建议

- 推荐采用「`main` 中的项目级统一基线 + 模块级独立扩展 Skill」。
- `main` 统一维护 `AGENTS.md` 与通用 Tuya pytest 用例 Skill；上半身运动、底盘等模块特殊流程分别维护 Skill，任务需要时调用。
- 模块分支定期通过 rebase 或 merge 同步 `main`，禁止手工复制规则文件，避免规则漂移。

## 2026-07-28：Tuya 规则与技能基线整理

- 新增项目级 `AGENTS.md` 和 Tuya `.cursorrules`，用于统一目录职责、Excel 用例规范、fixture 生命周期与实机安全门控。
- 用 `tuya-pytest-case-authoring`、`tuya-pytest-registry-session` 替换两个旧 `elephant-*` Skill。
- 新增 `tuya-motion-test-authoring`，仅在上半身运动、Jog、增量、软限位、避碰等任务时使用，不作为全局强制流程。
- 三项 Skill 已通过 `quick_validate.py`（以 `python -X utf8` 运行）；`git diff --check` 通过。
- 未修改、暂不处理用户已有的未跟踪测试文件和 `docs/Tuya_API_CN(1).md`。

## 2026-07-28：规则与技能分支同步完成

- `main` 已纳入规则、技能和项目记录。
- `feature/upper_body` 已快进至 `main`。
- `feature/chassis` 已完成对 `main` 的合并；合并前因 `test_data/upper_body.xlsx` 被 Excel 占用而中止一次，关闭占用后已恢复受控版本并顺利完成。
- 已推送至 `origin`：`main`、`feature/upper_body` 与 `feature/chassis` 均已发布（2026-07-28）。
