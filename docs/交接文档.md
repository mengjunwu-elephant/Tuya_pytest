# 项目交接文档

## 2026-08-01：set_upper_end_type 补超限

- normal 保持左/右/双臂 × `0`、`1` 共 6 条。
- exception：左/右/双臂 × `-1`、`2` 共 6 条，断言 `TuyaRobotSingleArmDataException`。
- 基线更新为 157 函数 / 1020 条。

## 2026-08-01：set_upper_movement_type 全范围与超限

- normal：左/右/双臂 × `movement_type=0~4` 共 15 条；双臂继续 `left=`/`right=`。
- exception：左/右/双臂 × `-1`、`5` 共 6 条，断言 `TuyaRobotSingleArmDataException`。
- 基线更新为 156 函数 / 1014 条。

## 2026-08-01：世界参考系改为仅总入口

- `get_upper_world_reference`：仅双臂 1 条结构断言（SDK 无单臂入口）。
- `set_upper_world_reference`：normal 全 0 共 1 条；exception 六轴超软限位正负各 1 条（X/Y/Z=±1001，RX/RY/RZ=±181），断言 `TuyaRobotDualArmDataException`。
- 调用改为 `upper_body.set_upper_world_reference(coords)`，去掉错误的 `left=`/`right=` 与 Arm 入口。
- 基线更新为 155 函数 / 999 条。

## 2026-08-01：补全 get/set_upper_filter_len

- `get_upper_filter_len`：normal 左/右/双臂 × `rank=1~5`（15）；exception `rank=0/6`（6）。
- `set_upper_filter_len`：normal 全 rank×`value=10`（15）+ `rank=1`×`value=1/255`（6）；exception `rank=0/6` 与 `value=0/256`（12）；`TuyaRobotSingleArmDataException`。
- 基线更新为 154 函数 / 991 条。

## 2026-08-01：set_upper_vr_mode 补全 mode=1 与超限

- normal：左/右/双臂 × `mode=0`、`1` 共 6 条；双臂调用 `set_upper_vr_mode(mode, mode)`。
- exception：左/右/双臂 × `mode=-1`、`2` 共 6 条，断言 `TuyaRobotSingleArmDataException`。
- 基线更新为 152 函数 / 943 条。

## 2026-08-01：新增 upper_go_zero 刷新/插补回零

- 新增 `test_upper_go_zero.py` 与 Excel sheet：左/右/双臂 × 插补/刷新共 6 条。
- 每条先到坐标初始姿态，再按模式调用 SDK `upper_go_zero`；插补期望返回 `0` 且同步等待，刷新期望返回 `1` 并轮询静止；回读零位角度后恢复模式并用 `device.go_zero()` 兜底。

## 2026-08-01：清错改为 Jog 触发后清除验证

- `clear_upper_error`：左/右/双臂各覆盖「坐标无解(32)」与「直线运动无相邻解(33)」共 6 条。
- 流程：坐标初始点位 → `upper_jog_coord` 触发报错 → `get_upper_robot_status` 确认含 `soft_error`/`motor_errors` → `clear_upper_error(254)` 断言返回 `1` → 再读状态为无报错 → 回零；标记 `motion + manual + danger`。

## 2026-08-01：关节使能补充非法参数

- `set_upper_joint_enable` 新增双臂 `exception` 4 条：`joint_id=0/9`、`state=-1/2`，断言 `TuyaRobotDualArmDataException`；正常用例仍为左/右/双臂 `joint_id=254` 失能。

## 2026-08-01：上电状态查询覆盖上下电

- `is_upper_powered_on`：同一函数内对左/右/双臂先下电断言 `0`/`[0,0]`，再上电断言 `1`/`[1,1]`；`finally` 恢复双臂上电。
- 不加 `danger`；Excel 使用 `expect_powered_off` / `expect_powered_on`。

## 2026-08-01：set_joint_min/max_angle 改为全局限位固定点覆盖

- 只调 `upper_body`；normal：J1～J7 各 5 个限位内固定角度（共 35）；回读全局 get 后直接 `==`。
- `finally` 与 module 结束均恢复 `TuyaRobotBase.UPPER_BODY_JOINT_SOFT_LIMITS`。
- exception 各 4 条：`joint_id=0/9`，J1 `degree=±167`，断言 `TuyaRobotDualArmDataException`。
- 基线更新为 151 函数 / 934 条。

## 2026-08-01：关节加速度边界与超限


- `set_upper_joint_acc` normal：左/右/双臂 × `acc=0.001`、`0.1`（`joint_id=1`）。
- exception：左/右/双臂 × `acc=0`、`0.101`；去掉 `joint_id=8` 与 `acc=100`。

## 2026-08-01：关节 min/max 角度查询改为全局限位并对照软限位


- `get_upper_joints_min_angle` / `get_upper_joints_max_angle` 各保留 1 条，只调 `upper_body`（不区分左右臂）。
- 断言返回长度 8；J1～J7 直接等于 `TuyaRobotBase.UPPER_BODY_JOINT_SOFT_LIMITS` 的下/上限，无容差；J8 不断言数值。
- 基线更新为 862 条。

## 2026-08-01：关节加速度 get/set 对齐 SDK 契约

- `get_upper_joint_acc`：左/右/双臂各 1 条，无参，断言单臂 7 元 float / 双臂 left+right 各 7 元。
- `set_upper_joint_acc`：normal 6 条（左/右/双臂 × joint_id 1、7，acc=0.1），刷新模式设置并按关节下标回读；exception 6 条（joint_id=8、acc=100 × 三目标），断言 `TuyaRobotSingleArmDataException`。
- 基线更新为 149 函数 / 866 条。

## 2026-08-01：upper_stop 改为运动中停止 + 刷新模式库报错

- 插补 3 条（左/右/双臂）：回零后 `_async=True` 下发坐标初始姿态，`sleep(0.3)` 后 `upper_stop`，断言返回 `1` 且 `is_moving` 为 `0`/`[0,0]`。
- 刷新 3 条（左/右/双臂）：切刷新后调 `upper_stop`，断言失败 `CommandResult` 且 message 含「刷新模式不支持stop」。
- 均为 `manual`；marker：`motion + manual + danger`。基线更新为 148 函数 / 863 条。

## 2026-08-01：暂停状态查询改为仅插补模式验证

- `get_upper_is_paused`：左/右/双臂各 1 条；只在插补模式验证，不切刷新（刷新不支持暂停的库报错留给 `upper_pause`）。
- 双臂插补并回零后，以 `_async=True` 下发坐标初始姿态，`sleep(0.3)` 后 `upper_pause`，断言暂停为 `1`（双臂 `[1,1]`），再 `upper_resume`，最后恢复插补并回零。

## 2026-07-31：运动状态查询改为刷新模式行程验证

- `get_upper_is_moving`：左/右/双臂各 1 条；先回零，切刷新模式并 `set_upper_motion_async(True)`，异步下发坐标初始姿态。
- 下发后 `sleep(0.1)` 断言运动中为 `1`（双臂 `[1,1]`），轮询至静止断言 `0`（双臂 `[0,0]`），最后恢复模式并回零。

## 2026-07-31：到位查询改为初始点位边界/超限

- `get_upper_is_in_position` 改为先运动到坐标初始点位，再按当前回读角度/坐标构造查询目标。
- 代表轴：角度 J1（±1° / ±1.1°）、坐标 X（±2 / ±2.1 mm）、姿态 RX（±0.3° / ±0.4°）；左右臂各 12 条，共 24 条。
- 边界为 `normal`（期望 `1`）、超限为 `exception`（期望 `0`，非 `pytest.raises`），分两个测试函数；模块级只运动到初始点位一次，用例内不再反复运动/回零。
- 标记 `motion + manual + danger`。

## 2026-07-31：坐标 Jog 结束态按实机结果调整

- `upper_jog_coord` 的 26 条 manual 预期不再统一要求 `32`“坐标无解”。
- 按 2026-07-31 实机结果：6 条业务返回 `0`（只断言返回值）；9 条断言 `33`“直线运动无相邻解”并回读停止坐标；11 条仍断言 `32`“坐标无解”。
- 已同步 Excel、`test_upper_jog_coord.py`、运动方案/交接文档、仓库规则与 motion 技能；未在本次改动中跑真机复验。

## 2026-07-30：老化脚本最新软件限位同步

- 已更新独立老化包 `scripts/aging/tuya_aging/constants.py`：J2 为 `[-80°, 105°]`；新增完整坐标软件限位，其中 Y 为 `[-841, 841] mm`、Z 为 `[-636, 665] mm`。
- `run_joint_limits()` 与 `run_jog_angles()` 直接读取 `JOINT_SOFT_LIMITS`，因此后续老化会使用新的 J2 正负边界；`--validate-config` 现在同时输出关节和坐标限位。
- 当时坐标 Jog 仍保持到状态码 `32`“坐标无解”的安全策略，未改为坐标软限位终点；该策略已于 2026-07-31 按实机结束态调整。
- 已通过 `python -m compileall -q scripts/aging`、`python scripts/aging/validate_tuya_aging.py`（14 项）和 `python scripts/aging/tuya_robot_aging.py --validate-config`；未运行真机。

## 2026-07-29：刷新模式运动默认异步全量覆盖

- 已在 `send_upper_coord`、`send_upper_angles`、`send_upper_coords` 的刷新/插补模式切换后显式设置并断言 `set_upper_motion_async(fresh_mode == 1)`。
- 已在 `upper_jog_angle`、`upper_jog_coord` 的刷新模式不支持验证中，在运动调用前显式执行 `set_upper_motion_async(True)`；清理时恢复插补同步状态再回零。
- 发送用例恢复运动前的默认异步状态，避免污染后续用例；未运行真机。

## 2026-07-29：公共回零改为零位关节运动

- `TuyaRobotBase.go_zero()` 不再调用 SDK `upper_go_zero()`，改为一次调用双臂 `send_upper_angles`，向左右臂下发 `UPPER_BODY_ZERO_ANGLES`。
- `TuyaRobotBase.go_arm_zero()` 同样改为只向指定单臂调用 `send_upper_angles`。
- 两个入口继续使用 `TuyaRobotBase.speed`、`result_data()` 和 `wait_upper(timeout=...)`，因此调用方和超时行为不变；当前代码中的速度值为 `50`。
- 当前 SDK 已核对：不存在 `send_angles`，本次使用其正式等价接口 `send_upper_angles`。规则、运动技能和运动方案已同步；未运行真机。

## 2026-07-29：上半身软件限位更新

- 用户提供最新限位截图：J2 `[-80°, 105°]`，Y `[-841, 841] mm`，Z `[-636, 665] mm`。
- 已同步统一常量、Excel 中的软件限位与超限用例，以及运动方案文档；未修改坐标 Jog 到“坐标无解”的场景。
- 已按用例 ID 复核 14 个代表性边界和超限值；未运行真机。

## 2026-07-29：刷新模式角度运动默认异步修复

- 用户提供的真机日志确认：左臂切到刷新模式后，未显式设置默认异步状态，`send_upper_angle` 被 SDK 以“无到位反馈”拦截。
- 已在 `testcases/upper_body/test_send_upper_angle.py` 的单臂、双臂和软件限位场景中增加 `set_upper_motion_async(fresh_mode == 1)` 及回读断言。
- 回零前固定切回双臂插补模式，以便 `go_zero()` 的同步等待可用；随后恢复用例前的左右臂刷新模式和默认异步状态。
- `python -m compileall -q testcases` 和目标模块收集（128 条）通过。全仓收集发现 9 个既有未跟踪测试文件缺少对应 Excel Sheet（含 `upper_motion_async`），与本次改动无关。
- 未运行真机；后续只需在受控环境下重新运行用户给出的单条刷新模式用例验证。

## 2026-07-29：恢复刷新模式异步默认值联动回归

- 当前分支：`feature/upper_body`。
- SDK 现状已核对：双臂 `set_upper_fresh_mode(mode)` 成功后同步默认异步状态为 `bool(mode)`；左右单臂入口不改变该全局状态。
- 已更新 `testcases/upper_body/test_set_upper_fresh_mode.py`：双臂用例回读并断言该联动，且始终恢复原全局异步状态，避免污染后续用例。
- 未修改项目规则或技能；已读取并遵守 `AGENTS.md`、`.cursorrules`、`tuya-pytest-case-authoring` 与 `tuya-motion-test-authoring`。
- 待验证：仅执行 `python -m compileall -q testcases` 与 `pytest testcases --collect-only -q`，不运行真机。

## 2026-07-28：同步远程 main

- 当前工作分支：`feature/chassis`。
- 已同步的远程跟踪分支：`origin/main`，提交为 `a2bb92d`（`add upper_body interface`）。
- 未将 `origin/main` 合并或变基到当前分支；这样可避免影响用户现有未跟踪文件。
- 项目内未发现既有规则或技能文件；本次未新增项目规则或技能。

## 2026-07-28：分支规则与技能核对

- `feature/chassis`：`.cursorrules`；`.cursor/skills/elephant-pytest-case-authoring/SKILL.md`；`.cursor/skills/elephant-pytest-registry-session/SKILL.md`。无 `AGENTS.md`。
- `feature/upper_body`：`AGENTS.md`、`.cursorrules`；`tuya-motion-test-authoring`、`tuya-pytest-case-authoring`、`tuya-pytest-registry-session` 三项 Skill。
- 两个分支的规则与 Skill 并非相同；当前分支的 `elephant-*` 配置应视为旧项目上下文，不应当作 TuyaRobot 的完整规则集。

## 2026-07-28：规则与技能治理建议

- 推荐采用「`main` 中的项目级统一基线 + 模块级独立扩展 Skill」。
- `main` 统一维护 `AGENTS.md` 与通用 Tuya pytest 用例 Skill；上半身运动、底盘等模块特殊流程分别维护 Skill，任务需要时调用。
- 模块分支定期通过 rebase 或 merge 同步 `main`，禁止手工复制规则文件，避免规则漂移。

## 2026-07-28：Tuya 规则与技能基线整理

- 新增项目级 `AGENTS.md` 和 Tuya `.cursorrules`，用于统一目录职责、Excel 用例规范、fixture 生命周期与实机安全门控。
- 用 `tuya-pytest-case-authoring`、`tuya-pytest-registry-session` 替换两个旧 `elephant-*` Skill。
- 新增 `tuya-motion-test-authoring`，仅在上半身运动、Jog、增量、软限位、避碰等任务时使用，不作为全局强制流程。
- 三项 Skill 已通过 `quick_validate.py`（以 `python -X utf8` 运行）；`git diff --check` 通过。
- 未修改、暂不处理用户已有的未跟踪测试文件和 `docs/Tuya_API_CN(1).md`。

## 2026-07-28：规则与技能分支同步完成

- `main` 已纳入规则、技能和项目记录。
- `feature/upper_body` 已快进至 `main`。
- `feature/chassis` 已完成对 `main` 的合并；合并前因 `test_data/upper_body.xlsx` 被 Excel 占用而中止一次，关闭占用后已恢复受控版本并顺利完成。
- 已推送至 `origin`：`main`、`feature/upper_body` 与 `feature/chassis` 均已发布（2026-07-28）。
